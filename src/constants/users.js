// Replaces magic strings scattered through the UI.
export const TRANSFER_TYPE = {
  INCREASE: 'increase',
  DECREASE: 'decrease',
};

export const RESET_TYPE = {
  LOGIN: 'password',
  PAYMENT: 'paymentPassword',
};

export const USERS_PAGE_SIZE = 20;
export const MIN_PASSWORD_LENGTH = 6;
